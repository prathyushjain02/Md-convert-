# Microsoft MarkItDown does all of the document -> Markdown conversion.
# The Azure extras from markitdown[all] are deliberately left out: they pull in
# the whole Azure SDK and this app does not use Document Intelligence.
markitdown[pdf,docx,pptx,xlsx,xls,outlook,audio-transcription,youtube-transcription]==0.1.7

Flask==3.1.3
gunicorn==26.1.0
